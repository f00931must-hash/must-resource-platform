# 多位老師設定教學 v4.2

1. 上傳 v4.2 到 GitHub。
2. 到 Firebase → Firestore Database → Rules。
3. 貼上 `firebase/firestore.rules` 的內容並發布。
4. 用 `master004400@gmail.com` 登入後台。
5. 到「系統設定」新增老師姓名、Email、權限。

其他老師之後直接用同一個 Google 登入按鈕登入即可。
